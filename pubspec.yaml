name: daftar
description: تطبيق إدارة الحسابات والديون لحرفيي الصاغة والمجوهرات
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  
  # تم تحديث المكتبات وإزالة screenshot لحل مشكلة البناء
  share_plus: ^10.0.0
  path_provider: ^2.1.4
  pdf: ^3.11.1
  printing: ^5.13.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
